{
  "name": "nexus-amo-widget",
  "version": "0.1.0",
  "private": true,
  "description": "Nexus Looker — preview attachments inline in amoCRM (no download).",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist/*",
    "test": "vitest run"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.23.0",
    "express": "^4.22.2",
    "jquery": "^4.0.0",
    "jsdom": "^25.0.1",
    "p-limit": "^6.2.0",
    "supertest": "^7.2.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "markdown-it": "^14.2.0"
  }
}
