# Init: amo_looker

**Дата:** 2026-06-10. Параметры выведены из контекста проекта (handoff, разведка, существующий репо) — пользователь дал разрешение работать без промежуточных вопросов.

## Сырое описание

Приватный amoCRM-виджет «Nexus Looker»: кнопка-«глазик» рядом с каждым файлом в ленте карточки сделки/контакта/компании → модалка с предпросмотром (PDF, картинки, DOCX, XLSX, текст; legacy .doc/.xls/.ppt через серверный LibreOffice-конвертер). Замена платного CatCode Looker (тарифицирует всех пользователей аккаунта). Маркетплейс не нужен — один прод-аккаунт `toolkeeper.amocrm.ru`.

Есть handoff от Михаила (владелец, foringella@gmail.com): скаффолд виджета (шаг 1 из 0–8 готов), план шагов, известные блокеры. Материалы разобраны и сохранены в `reuse/` (см. `reuse/INVENTORY.md`).

## Параметры

- Репо: `gram2Claude/amo_looker` — **уже существует** (master защищён, dev-ветка `oleg`).
- Старт плана: 2026-06-11 (следующий рабочий день после планирования).
- Календарь RU, 8ч/день, пн–пт, буфер 1.30.
- Plane: workspace `nexus_system`, identifier `AMO` (создаётся на publish).
- Тип: lib (артефакт — zip виджета; гейт: npm ci + npm run build; смоук — ручной в кабинете amoCRM).

## Выполнено до плана (вне календаря)

- Каркас виджета (handoff, шаг 1) — сборка esbuild → zip работает.
- DOM-разведка живого кабинета venskons78.amocrm.ru (шаг 0): селекторы ленты, два формата URL вложений, рабочий рецепт `fetch(credentials:'same-origin')` — `work_directory/01_specs/01_dom_recon_amocrm.md`.
- Инвентаризация handoff → `reuse/` (коммит 4b908b4).
