# Реконсиляция ревью реализации E4 (geo-performance)

**Дата:** 2026-06-12 · **Предмет:** все коммиты oleg сверх master (warm-пул, тайминг-логи, http2, гибрид, LRU-кэш, preconnect)
**Ревьюеры:** независимый Claude-субагент (fresh context, **8/10**), OpenAI Codex gpt-5.5 (**7/10**), security-review (субагент-анализ + фильтрация)

## Сводка решений

| # | Находка | Severity | Источник | Решение |
|---|---|---|---|---|
| 1 | `mkdtemp` между `acquire()` и `try` → при ошибке ФС (ENOSPC) воркер навсегда busy; 2 ошибки = перманентная смерть пула | HIGH | агент | **Исправлено**: всё после acquire — в try/finally с release; `_mkdtemp` DI + регресс-тест |
| 2 | Кэш-хит может создать objectURL после `Loader.dispose()` (синхронный путь, быстрый open-close) → утечка blob-URL | HIGH | codex (агент — как pre-existing LOW) | **Исправлено**: флаг `_disposed`; `objectURL`/`fetchBuffer`/`post` на disposed-лоадере бросают AbortError (рендереры глотают); тесты |
| 3 | Колбэки жизненного цикла воркера не привязаны к поколению процесса: stale boot-таймер мог пометить НОВЫЙ процесс ready, запоздавший exit убитого — пометить новый dead (конфликт порта) | MEDIUM | оба | **Исправлено**: generation-guard `w.proc === proc` во всех колбэках, clearTimeout boot-таймера на kill/shutdown, `w.proc = null` после kill; регресс-тест stale-таймера |
| 4 | spawn-error воркера → вечный молчаливый respawn-цикл | LOW | агент | **Исправлено**: лог `worker-spawn-error` (backoff не добавлен — respawn 1с при нуле пользователей приемлем) |
| 5 | Гибрид `server.js` не покрыт тестами (логика в entry-point) | LOW | агент | **Исправлено**: вынесен в `makeHybridConvert` (warm.js) + тест порога |
| 6 | Дыры покрытия: MAX_JOBS-recycle, ENOSPC-release, disposed-loader | LOW | оба | **Исправлено**: +5 тестов (итого 90) |
| 7 | Cache stampede при конкурентных открытиях одного href (двойной POST на конвертер) | MEDIUM | codex | **Отклонено**: модалка — синглтон, `open()` сначала зовёт `close()` (abort предыдущего); конкурентные рендеры одного href через UI недостижимы; худший случай — лишний фетч в пределах rate-limit. In-flight коалесцентность с refcount-abort — сложность без достижимого сценария (агент-ревьюер подтвердил «практически недостижим») |
| 8 | RAM worst-case гибрида: тёплые воркеры + холодные soffice + тела запросов под mem_limit 2g | MEDIUM | codex | **Принято документированием**: p-limit капит суммарную конкурентность конвертаций двумя; worst-case задокументирован в compose; OOM cgroup перезапускает контейнер (restart policy) — деградация ограничена. Нагрузочный тест — при появлении реального трафика |
| 9 | fail-streak не различает «битый файл» от «деградировавший воркер» → recycle здорового воркера парой мусорных документов | MEDIUM-low | агент | **Принято как есть**: смягчено rate-limit'ом (30/мин на кабинет) и env-тюнингом `POOL_FAIL_STREAK` без передеплоя; цена ложного recycle — 4с тёплой ёмкости |
| 10 | SIGTERM → мгновенный exit, обрыв in-flight конвертаций при docker stop | LOW | агент | **Принято как есть** при нуле пользователей; graceful-shutdown — кандидат в backlog перед реальным трафиком |
| 11 | Warm-пул ослабляет per-document изоляцию LO (тёплый воркер живёт между джобами разных кабинетов; прекондиция — 0-day RCE в парсерах LO) | INFO (~5/10) | security | **Принято как trade-off**: профилактический recycle (100 джобов) ограничивает окно; при желании ужесточается `POOL_MAX_JOBS` без кода |
| 12 | `pip3 install unoserver` не запинен (supply-chain при сборке образа) | INFO | security | **Принято**: исключённая категория (hardening); кандидат — запинить при следующей пересборке |

## Security-review

**Высоконадёжных уязвимостей не найдено** (0 findings ≥ 8/10). Подтверждены инварианты: command-injection в spawn-аргументах нет (константы + whitelist ext до вызова), `requireAuth` до `express.raw` цел (timing-middleware тело не читает), имена файлов/IP в логах отсутствуют на всех путях, кэш хранит буферы (не objectURL) — revoke-семантика цела, preconnect не несёт пользовательского ввода, build-guard проходит.

## Верификация после фиксов

- 90/90 тестов зелёные; `npm run build` (build-guard) проходит; sha256 `releases/nexus-looker-0.2.0.zip` не изменился (`9e099e1d…`).
- Конвертер передеплоен с фиксами: health OK, warm smoke 0.26–0.64с.
- Dev-статика `/widget-dev/` обновлена пересобранным 0.3.0 (с фиксом disposed-loader).

## Открытые пункты (не блокируют merge)

1. **Browser-e2e 0.3.0 через dev-букмарклет** (T28): unit-покрытие нового клиентского кода полное, 0.2.0 e2e-проверен утром 12.06; живой прогон 0.3.0 по матрице форматов через `/widget-dev/` — ручной шаг (букмарклет с BASE=/widget-dev) или следующая сессия.
2. Гейт CDN — отложен до появления точки замера SEA/US (действие владельца: VPS).
