# Ревью кода спринта E1S1 (AMO-1/2/3): субагент + codex

**Дата:** 2026-06-10. Дифф: `master...oleg` (перенос виджета, фикс credentials, vendor, манифест). Два независимых ревьюера: субагент Claude (полный аудит переноса) и OpenAI Codex (adversarial, дифф без vendor).

## Вердикты

- **Субагент:** «Спринт выполнен добротно: фикс credentials полный и доказанный (включая задеплоенный бандл), перенос без потерь (27 файлов сверены с reuse-коммитом), zip корректной структуры. Блокеров для E1S2 нет».
- **Codex:** 2×[P1] + 3×[P2]; ни один не блокирует merge спринта (см. разбор).

## Сводный разбор находок

| # | Ревьюер | Находка | Решение |
|---|---|---|---|
| 1 | субагент | **xlsx.full.min.js: ИМЕНОВАННЫЙ `define("xlsx")`** — в AMD-окружении фабрика молча не выполнится, `window.XLSX` останется truthy-заглушкой `{}`; guard `if (window.XLSX)` замаскирует поломку навсегда | ✅ Внесено в канон T10 (guard → `window.XLSX && window.XLSX.read`, шэдоуинг define) |
| 2 | субагент | jszip — тоже anonymous define; трюк с занулением define нужен и ему; глобальное зануление = гонка с AMD-загрузками amo → рассмотреть fetch+eval с локальным шэдоуингом | ✅ Внесено в канон T9 |
| 3 | субагент | README качал vendor без пина версий (unpkg latest) | ✅ Исправлено сразу: версии запинены + добавлен jszip |
| 4 | субагент | При ручном require-smoke `params` не установлен → vendor URL уйдёт в 404 | ✅ Внесено в T9 (подставлять params.path руками) |
| 5 | codex | [P1] build.js упадёт без images/ на чистом checkout | ❌ FALSE POSITIVE: images/*.png закоммичены в AMO-1 (бинарники не видны в текстовом диффе, который получил codex) |
| 6 | codex | [P1] docx/xlsx не проверяют `r.ok` перед чтением тела → мутные ошибки рендера | ☑️ Уже покрыто планом: T6 (единый fetch-слой, идёт ДО T9/T10) делает r.ok/byteLength/i18n-ошибки |
| 7 | codex | [P2] modal.js:43 `\|\| 'legacy'` — неизвестный формат уедет на внешний конвертер (утечка файла) | ✅ Внесено в канон T17 (fallback → unsupported, legacy только для распознанных) |
| 8 | codex | [P2] legacy.js: createObjectURL без revoke | ☑️ Уже покрыто T6 (revokeObjectURL в loader) |
| 9 | codex | [P2] **sheet_to_html → $sheet.html(): потенциальный XSS** через содержимое ячеек workbook | ✅ Внесено в канон T10 (санитизация / DOM-рендер) |

Прочее от субагента (мелочи, без действий): остаточный `widget.advanced.title` в i18n (пригодится в T17); `npm run clean` не работает в cmd (build.js сам чистит); zip+скриншоты пухнут в git (осознанно); AMO-2 как точка истории самопротиворечив (HEAD консистентен).

## Гигиена

Секретов в коммитах нет (grep + просмотр скриншотов с ключами — чисто; код виджета в URL CDN публичен по природе). .gitignore покрывает node_modules/dist.

## Итог

Спринт готов к merge-гейту: оба ревью без блокеров, все находки либо исправлены на месте (README), либо зафиксированы в канон-задачах будущих спринтов (T6/T9/T10/T17), где они и должны решаться.
